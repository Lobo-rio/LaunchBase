{% extends "./layout.njk" %}


{% block titlePage %}
    <title>Update Students - SchoolFY</title>
{% endblock titlePage %}

{% block styles %}
    <link rel="stylesheet" href="/styles/section.css">
{% endblock styles %}

{% block container %}

    <div class="main-create">
        <div class="create-avatar">
            <img src="{{ student.avatar_url }}" alt="Image Student {{ student.mane }}">
        </div>
        <div class="create-information">
            <h1>Alterar Dados do Professor(a)</h1>
            <form action="/students?_method=PUT" method="POST">
                {% include "students/partials/fields.njk" %}
                <input type="hidden" name="id" value="{{ student.id }}">
                <button class="save" type="submit">Alterar</button>
            </form>
            <form action="/students?_method=DELETE" method="POST">
                <input type="hidden" name="id" value="{{ student.id }}">
                <button class="delete" type="submit">Excluir</button>
            </form>
        </div>
    </div>
{% endblock container %}